name: Build Windows exe

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build exe
        run: |
          pyinstaller --onefile --name bandobot main.py

      - name: Assemble release folder
        run: |
          mkdir release
          copy dist\bandobot.exe release\
          copy .env.example release\.env.example
          mkdir release\data\users
          mkdir release\data\history
          type nul > release\data\users\.gitkeep
          type nul > release\data\history\.gitkeep

      - uses: actions/upload-artifact@v4
        with:
          name: bandobot-windows
          path: release/

      - name: Zip release folder
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          Compress-Archive -Path release\* -DestinationPath bandobot-windows.zip

      - name: Publish GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: bandobot-windows.zip

